# ملعب مشكاة (Mishkah Playground)

يحتوي هذا المجلد على تطبيقات تجريبية وأمثلة تم بناؤها باستخدام **إطار عمل مشكاة (Mishkah Framework)**. يعمل هذا المكان كساحة اختبار للميزات الجديدة، والمكونات، والأنماط المعمارية.

## 📚 تطبيق الويكي (Wiki App)

**تطبيق ويكي مشكاة** (`wiki-app.js`) هو قاعدة معرفية كاملة الوظائف وتعمل دون إنترنت. يوضح هذا التطبيق قوة معمارية مشكاة في بناء تطبيقات معقدة تعتمد على البيانات دون الحاجة لأدوات بناء ثقيلة.

### الميزات الرئيسية

* **دعم ثنائي اللغة**: دعم أصلي للمحتوى العربي والإنجليزي (`ar` / `en`)، مع تحويل اتجاه الواجهة (`rtl` / `ltr`).
* **العمل دون اتصال**: يستخدم `IndexedDB` (عبر `MishkahIndexedDB`) لتخزين المقالات محلياً.
* **هيكل شجري**: يمكن للمقالات أن تمتلك آباء وأشقاء، مما يشكل شبكة معرفية.
* **السمات (Themes)**: دعم مدمج للوضع الداكن (Dark) والفاتح (Light).
* **بدون تعقيدات بناء**: يعمل مباشرة في المتصفح باستخدام ES5/ES6.

### هيكل الملفات

* `wiki-app.js`: يحتوي على منطق التطبيق الأساسي، إدارة الحالة، وعرض الواجهة (عبر Mishkah DSL).
* `wiki-data.js`: يحتوي على البيانات الأولية (مقالات البداية) التي تغطي أطر العمل (React, Vue, Angular, إلخ).
* `wiki.html`: ملف HTML الرئيسي للدخول للتطبيق.
* `wiki-ui.js`: (إن وجد) مكونات واجهة إضافية للويكي.

### كيفية الاستخدام

1. افتح `wiki.html` في متصفحك.
2. استخدم الشريط الجانبي للتنقل بين المواضيع.
3. اضغط على "مقالة جديدة" لإضافة محتوى.
4. بدّل اللغة/السمة من الشريط الجانبي أو الترويسة.
5. يتم حفظ البيانات في IndexedDB الخاص بالمتصفح.

## 🧪 تجارب أخرى

* **العدادات (Counters)**: `example-counter.js`, `wiki-counter.js` - أمثلة بسيطة لإدارة الحالة.
* **CPS**: `example-cps.js`, `wiki-cps.js` - تجارب لأسلوب Continuation-Passing Style أو أمثلة للتحكم المعقد في التدفق.

## 🤝 المشاركة

لا تتردد في إضافة تجاربك الخاصة هنا. يرجى اتباع النمط الحالي للملفات المستقلة (Self-contained files).
